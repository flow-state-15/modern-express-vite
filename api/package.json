{
  "name": "absi-inventory",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "tsc -b && node dist/index.js",
    "dev": "tsc -b && nodemon --exec node dist/index.js",
    "build": "tsc -b",
    "test": "npm run build && NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules jest --config jest.config.cjs",
    "test:watch": "NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules jest --watch --config jest.config.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1",
    "nodemon": "^3.1.11"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/morgan": "^1.9.10",
    "@types/node": "^24.10.9",
    "@types/supertest": "^2.0.16",
    "jest": "^30.1.3",
    "supertest": "^7.1.4",
    "typescript": "^5.9.3"
  }
}
