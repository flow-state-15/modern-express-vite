# Copy to .env.test and set real values. Overrides .env when running tests.
# Use localhost (not db) when running tests from host; use same creds as .env.db / postgres.
NODE_ENV=test
DATABASE_URL=postgresql://user:password@localhost:5432/database
SESSION_SECRET=your-session-secret
